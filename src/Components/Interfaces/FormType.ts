interface FormType {
    type?: string,
    id?: string,
    name: string,
    placeholder?: string,
    pattern?: string,
    as?: any,
    cols?: number,
    rows?: number,
    className?: string
};

export default FormType;